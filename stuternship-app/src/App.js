import { BrowserRouter as Routes, Router, Route } from 'react-router-dom';
import Home from './routes/signup';

export default function App() {
  return (
    <>
      {/* <Router>
        <Routes>
          <Route path='/' component={<Home />} />
        </Routes>
      </Router> */}
    </>
  );
}
